# Local-only example configuration.
# Copy to .env for development; do not commit real secrets.

SPRING_APPLICATION_NAME=trasck
SERVER_PORT=6100
CORS_ALLOWED_ORIGINS=http://localhost:8080

SPRING_DATASOURCE_URL=jdbc:postgresql://trasck-db:5432/trasck
SPRING_DATASOURCE_DRIVER=org.postgresql.Driver
POSTGRES_USER=trasck
POSTGRES_PASSWORD=replace-with-local-db-password
POSTGRES_DB=trasck

SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=true
SPRING_JPA_OPEN_IN_VIEW=false

# Local values may be generated with:
# openssl rand -base64 48
TRASCK_JWT_SECRET=replace-with-at-least-32-characters
TRASCK_SECRETS_ENCRYPTION_KEY=replace-with-at-least-32-characters
TRASCK_OAUTH_ASSERTION_SECRET=replace-with-at-least-32-characters
TRASCK_AUTH_COOKIE_SECURE=false
TRASCK_OAUTH_SUCCESS_REDIRECT=http://localhost:8080/auth/callback
TRASCK_TRUST_FORWARDED_FOR=false
TRASCK_SECURITY_RATE_LIMIT_STORE=database

# Required only when TRASCK_SECURITY_RATE_LIMIT_STORE=redis.
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379
SPRING_DATA_REDIS_PASSWORD=

# Comma-separated exact host, host:port, wildcard host, wildcard host:port, or CIDR entries for trusted self-hosted outbound targets.
# Examples: hooks.example.com,worker.internal:8443,*.trusted.example.com,*.trusted.example.com:8443,10.0.0.0/8,fd00::/8
# Keep empty unless a local webhook, worker, or S3-compatible endpoint is intentionally trusted.
TRASCK_OUTBOUND_URL_ALLOWED_HOSTS=

TRASCK_MULTIPART_MAX_FILE_SIZE=10MB
TRASCK_MULTIPART_MAX_REQUEST_SIZE=12MB
TRASCK_ATTACHMENTS_MAX_UPLOAD_BYTES=10485760
TRASCK_ATTACHMENTS_MAX_DOWNLOAD_BYTES=52428800
TRASCK_ATTACHMENTS_ALLOWED_CONTENT_TYPES=text/plain,text/markdown,text/csv,application/json,application/pdf,image/png,image/jpeg,image/gif,image/webp,application/zip,application/octet-stream
TRASCK_EXPORTS_MAX_ARTIFACT_BYTES=52428800
TRASCK_EXPORTS_ALLOWED_CONTENT_TYPES=application/json,text/csv,application/octet-stream
TRASCK_IMPORTS_MAX_PARSE_BYTES=5242880
TRASCK_IMPORTS_ALLOWED_CONTENT_TYPES=text/csv,application/csv,application/json,text/json,text/plain

SPRING_MAIL_HOST=maildev
SPRING_MAIL_PORT=1025
SPRING_MAIL_USERNAME=
SPRING_MAIL_PASSWORD=
TRASCK_EMAIL_PROVIDER=maildev
TRASCK_EMAIL_FROM=no-reply@trasck.local
